--[[
    Blox Fruits Hub (Tabbed Menu - Fixed Crosshair Camlock, Ally Filter, Enemy TP, Auto Raid)
]]

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local localPlayer = Players.LocalPlayer
local camera = Workspace.CurrentCamera
local saveFileName = "BloxFruitHubSettings_" .. localPlayer.Name .. ".json"

local function nukeEverythingOld()
    local namesToDestroy = {
        "KeybindSettingUI", "PvPHUDUI", "PlayerESP", "StandalonePlayerESP", 
        "AlwaysActivePlayerESP", "BloxFruitHub"
    }
    
    for _, obj in ipairs(CoreGui:GetChildren()) do
        for _, targetName in ipairs(namesToDestroy) do
            if obj.Name == targetName or obj.Name:find("BloxFruit") or obj.Name:find("Keybind") then
                pcall(function() obj:Destroy() end)
            end
        end
    end
    
    local playerGui = localPlayer:FindFirstChild("PlayerGui")
    if playerGui then
        for _, obj in ipairs(playerGui:GetChildren()) do
            for _, targetName in ipairs(namesToDestroy) do
                if obj.Name == targetName or obj.Name:find("BloxFruit") or obj.Name:find("Keybind") then
                    pcall(function() obj:Destroy() end)
                end
            end
        end
    end
end

nukeEverythingOld()
task.wait(0.2)

local Keybinds = {
    PlayerCam = Enum.KeyCode.P,
    NpcCam = Enum.KeyCode.N,
    SuperJump = Enum.KeyCode.L,
    LongDash = Enum.KeyCode.H,
    TpEnemy = Enum.KeyCode.T,
    MenuToggle = Enum.KeyCode.M,
    MenuHide = Enum.KeyCode.B
}

local FastAttackSettings = { AutoClick = false, ClickDelay = 0.08, Distance = 85 }
local AutoRaidSettings = { Enabled = false, FlySpeed = 180 }
local ESPStates = { Enabled = true }
local CombatStates = { PlayerCamActive = false, NpcCamActive = false, LockedTarget = nil }

local function getRegisterAttack()
    local success, res = pcall(function() return ReplicatedStorage.Modules.Net["RE/RegisterAttack"] end)
    if success and res then return res end
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v.Name == "RegisterAttack" or v.Name == "RE/RegisterAttack" then return v end
    end
    return nil
end

local function getRegisterHit()
    local success, res = pcall(function() return ReplicatedStorage.Modules.Net["RE/RegisterHit"] end)
    if success and res then return res end
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v.Name == "RegisterHit" or v.Name == "RE/RegisterHit" then return v end
    end
    return nil
end

local function executeMultiAttack(targetsList)
    pcall(function()
        local char = localPlayer.Character
        if not char then return end
        local equippedWeapon = char:FindFirstChildOfClass("Tool")
        if equippedWeapon then
            local attackRemote = equippedWeapon:FindFirstChild("LeftClickRemote") or equippedWeapon:FindFirstChild("ToolAttack") or equippedWeapon:FindFirstChild("AttackRemote")
            if attackRemote and typeof(attackRemote.FireServer) == "function" and #targetsList > 0 then
                local firstRoot = targetsList[1].Root
                local direction = (firstRoot.Position - char:GetPivot().Position).Unit
                attackRemote:FireServer(direction, 1)
            end
        end
        
        local regAttack = getRegisterAttack()
        if regAttack and typeof(regAttack.FireServer) == "function" then regAttack:FireServer(0) end
        
        local regHit = getRegisterHit()
        if regHit and typeof(regHit.FireServer) == "function" and #targetsList > 0 then
            local hitParams = {}
            for _, data in ipairs(targetsList) do
                if data.Head and data.Head.Parent then
                    table.insert(hitParams, {data.Head.Parent, data.Head})
                end
            end
            if #hitParams > 0 then
                regHit:FireServer(targetsList[1].Head, hitParams)
            end
        end
    end)
end

local function isValidRaidEnemy(mob)
    local success, result = pcall(function()
        if not mob or not mob.Parent then return false end
        local hum = mob:FindFirstChild("Humanoid")
        local root = mob:FindFirstChild("HumanoidRootPart")
        if not hum or not root then return false end
        if hum.Health <= 0 then return false end
        return true
    end)
    return success and result
end

local function findCurrentRaidIsland(rootPos)
    local bestTarget = nil
    local shortestDist = math.huge

    local enemiesFolder = Workspace:FindFirstChild("Enemies")
    if enemiesFolder then
        for _, mob in ipairs(enemiesFolder:GetChildren()) do
            if isValidRaidEnemy(mob) then
                local mobRoot = mob:FindFirstChild("HumanoidRootPart")
                if mobRoot then
                    local dist = (mobRoot.Position - rootPos).Magnitude
                    if dist < shortestDist then
                        shortestDist = dist
                        bestTarget = mobRoot.Position + Vector3.new(0, 20, 0)
                    end
                end
            end
        end
    end

    if bestTarget then return bestTarget end

    for _, obj in ipairs(Workspace:GetChildren()) do
        local name = obj.Name:lower()
        if name:find("island") or name:find("raid") or name:find("map") then
            if obj:IsA("Model") then
                local success, pivot = pcall(function() return obj:GetPivot().Position end)
                if success and pivot then
                    local dist = (pivot - rootPos).Magnitude
                    if dist < shortestDist and pivot.Y > -50 and pivot.Y < 3000 then
                        shortestDist = dist
                        bestTarget = pivot + Vector3.new(0, 40, 0)
                    end
                end
            end
        end
    end

    return bestTarget
end

local function manageRaidFlight()
    pcall(function()
        local char = localPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        local hum = char and char:FindFirstChild("Humanoid")
        if not root or not hum or hum.Health <= 0 then return end

        local bv = root:FindFirstChild("RaidFlyBV")
        local bg = root:FindFirstChild("RaidFlyBG")

        if AutoRaidSettings.Enabled then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = false
                end
            end

            if not bv then
                bv = Instance.new("BodyVelocity")
                bv.Name = "RaidFlyBV"
                bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
                bv.Velocity = Vector3.new(0, 0, 0)
                bv.Parent = root
            end
            if not bg then
                bg = Instance.new("BodyGyro")
                bg.Name = "RaidFlyBG"
                bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
                bg.CFrame = root.CFrame
                bg.Parent = root
            end

            local targetPos = findCurrentRaidIsland(root.Position)

            if targetPos then
                local direction = (targetPos - root.Position)
                if direction.Magnitude > 10 then
                    bv.Velocity = direction.Unit * AutoRaidSettings.FlySpeed
                    bg.CFrame = CFrame.new(root.Position, targetPos)
                else
                    bv.Velocity = Vector3.new(0, 0, 0)
                end
            else
                bv.Velocity = Vector3.new(0, 0, 0)
            end
        else
            if bv then bv:Destroy() end
            if bg then bg:Destroy() end
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end)
end

local function teleportBehindEnemy()
    pcall(function()
        local char = localPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then return end

        local closestRoot = nil
        local shortestDist = math.huge

        local enemiesFolder = Workspace:FindFirstChild("Enemies")
        if enemiesFolder then
            for _, mob in ipairs(enemiesFolder:GetChildren()) do
                if isValidRaidEnemy(mob) then
                    local enemyRoot = mob:FindFirstChild("HumanoidRootPart")
                    if enemyRoot then
                        local dist = (enemyRoot.Position - root.Position).Magnitude
                        if dist < shortestDist then
                            shortestDist = dist
                            closestRoot = enemyRoot
                        end
                    end
                end
            end
        end

        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= localPlayer and plr.Character then
                if not (localPlayer.Team and plr.Team and localPlayer.Team == plr.Team) then
                    local enemyRoot = plr.Character:FindFirstChild("HumanoidRootPart")
                    local hum = plr.Character:FindFirstChild("Humanoid")
                    if enemyRoot and hum and hum.Health > 0 then
                        local dist = (enemyRoot.Position - root.Position).Magnitude
                        if dist < shortestDist then
                            shortestDist = dist
                            closestRoot = enemyRoot
                        end
                    end
                end
            end
        end

        if closestRoot then
            local behindCFrame = closestRoot.CFrame * CFrame.new(0, 0, 4)
            root.CFrame = CFrame.new(behindCFrame.Position, closestRoot.Position)
        end
    end)
end

local function saveKeybinds()
    pcall(function()
        if writefile and HttpService then
            local encodedTable = {}
            for keyName, keyCode in pairs(Keybinds) do encodedTable[keyName] = keyCode.Name end
            writefile(saveFileName, HttpService:JSONEncode(encodedTable))
        end
    end)
end

local function loadKeybinds()
    pcall(function()
        if readfile and isfile and isfile(saveFileName) then
            local success, decoded = pcall(function() return HttpService:JSONDecode(readfile(saveFileName)) end)
            if success and type(decoded) == "table" then
                for keyName, enumName in pairs(decoded) do
                    if Keybinds[keyName] ~= nil and Enum.KeyCode[enumName] then Keybinds[keyName] = Enum.KeyCode[enumName] end
                end
            end
        end
    end)
end
loadKeybinds()

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KeybindSettingUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local OpenButton = Instance.new("TextButton")
OpenButton.Size = UDim2.new(0, 160, 0, 45)
OpenButton.Position = UDim2.new(0, 20, 0, 100)
OpenButton.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
OpenButton.TextColor3 = Color3.fromRGB(0, 255, 128)
OpenButton.TextSize = 15
OpenButton.Font = Enum.Font.SourceSansBold
OpenButton.Text = "Toggle Menu [M]"
OpenButton.Active = true
OpenButton.Parent = ScreenGui
Instance.new("UICorner", OpenButton).CornerRadius = UDim.new(0, 6)

local frameWidth, frameHeight = 560, 420
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, frameWidth, 0, frameHeight)
MainFrame.Position = UDim2.new(0.5, -frameWidth/2, 0.5, -frameHeight/2)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Visible = true 
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 45)
TitleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.TextSize = 18
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.Text = "   Blox Fruits Hub (Tabbed Menu)"
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = MainFrame
Instance.new("UICorner", TitleLabel).CornerRadius = UDim.new(0, 8)

local TabContainer = Instance.new("Frame")
TabContainer.Size = UDim2.new(0, 130, 1, -55)
TabContainer.Position = UDim2.new(0, 0, 0, 50)
TabContainer.BackgroundColor3 = Color3.fromRGB(25, 25, 32)
TabContainer.BorderSizePixel = 0
TabContainer.Parent = MainFrame

local ContentPanel = Instance.new("Frame")
ContentPanel.Size = UDim2.new(1, -140, 1, -60)
ContentPanel.Position = UDim2.new(0, 135, 0, 50)
ContentPanel.BackgroundTransparency = 1
ContentPanel.Parent = MainFrame

local function createContentScrollingFrame()
    local sf = Instance.new("ScrollingFrame")
    sf.Size = UDim2.new(1, 0, 1, 0)
    sf.BackgroundTransparency = 1
    sf.CanvasSize = UDim2.new(0, 0, 0, 400)
    sf.ScrollBarThickness = 4
    sf.Visible = false
    sf.Parent = ContentPanel
    return sf
end

local RaidFrame = createContentScrollingFrame()
local PvPFrame = createContentScrollingFrame()

local function createTabButton(name, yPos, targetFrame)
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 40)
    btn.Position = UDim2.new(0, 5, 0, yPos)
    btn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
    btn.Font = Enum.Font.SourceSansBold
    btn.TextSize = 14
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.Text = name
    btn.Parent = TabContainer
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    btn.MouseButton1Click:Connect(function()
        RaidFrame.Visible = false
        PvPFrame.Visible = false
        targetFrame.Visible = true
    end)
    return btn
end

createTabButton("Raid", 10, RaidFrame)
createTabButton("PvP", 60, PvPFrame)

RaidFrame.Visible = true

local function createToggleRow(parentFrame, name, initialState, yPos)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -10, 0, 34)
    Row.Position = UDim2.new(0, 5, 0, yPos)
    Row.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    Row.Parent = parentFrame
    Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.6, 0, 1, 0)
    Label.Position = UDim2.new(0, 10, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.SourceSansBold
    Label.TextSize = 13
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Text = name
    Label.Parent = Row

    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0, 90, 0, 26)
    Button.Position = UDim2.new(1, -95, 0.5, -13)
    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    Button.Font = Enum.Font.SourceSansBold
    Button.TextSize = 12
    Button.Text = initialState and "ON" or "OFF"
    Button.TextColor3 = initialState and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
    Button.Parent = Row
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 4)

    return Button
end

local function createKeybindRow(parentFrame, name, keyName, yPos)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, -10, 0, 34)
    Row.Position = UDim2.new(0, 5, 0, yPos)
    Row.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    Row.Parent = parentFrame
    Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.45, 0, 1, 0)
    Label.Position = UDim2.new(0, 10, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.SourceSansBold
    Label.TextSize = 12
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Text = name
    Label.Parent = Row

    local KeyButton = Instance.new("TextButton")
    KeyButton.Size = UDim2.new(0, 110, 0, 26)
    KeyButton.Position = UDim2.new(1, -115, 0.5, -13)
    KeyButton.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    KeyButton.Font = Enum.Font.SourceSansBold
    KeyButton.TextSize = 12
    KeyButton.Text = Keybinds[keyName].Name
    KeyButton.TextColor3 = Color3.fromRGB(0, 255, 128)
    KeyButton.Parent = Row
    Instance.new("UICorner", KeyButton).CornerRadius = UDim.new(0, 4)

    local listening = false
    KeyButton.MouseButton1Click:Connect(function()
        if listening then return end
        listening = true
        KeyButton.Text = "Press Key..."
        KeyButton.TextColor3 = Color3.fromRGB(255, 200, 0)
        
        local connection
        connection = UserInputService.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.Keyboard then
                Keybinds[keyName] = input.KeyCode
                KeyButton.Text = input.KeyCode.Name
                KeyButton.TextColor3 = Color3.fromRGB(0, 255, 128)
                listening = false
                saveKeybinds()
                if connection then connection:Disconnect() end
            end
        end)
    end)
end

local btnAutoRaid = createToggleRow(RaidFrame, "Auto Raid (Do Only)", AutoRaidSettings.Enabled, 5)

local btnFastAttack = createToggleRow(PvPFrame, "Fast Attack", FastAttackSettings.AutoClick, 5)
local btnESP = createToggleRow(PvPFrame, "ESP Toggle", ESPStates.Enabled, 45)

createKeybindRow(PvPFrame, "Player Camlock Key", "PlayerCam", 85)
createKeybindRow(PvPFrame, "NPC Camlock Key", "NpcCam", 125)
createKeybindRow(PvPFrame, "Super Jump", "SuperJump", 165)
createKeybindRow(PvPFrame, "Long Dash", "LongDash", 205)
createKeybindRow(PvPFrame, "Tp to Enemy", "TpEnemy", 245)
createKeybindRow(PvPFrame, "Hide/Unhide UI", "MenuHide", 285)

btnFastAttack.MouseButton1Click:Connect(function()
    FastAttackSettings.AutoClick = not FastAttackSettings.AutoClick
    btnFastAttack.Text = FastAttackSettings.AutoClick and "ON" or "OFF"
    btnFastAttack.TextColor3 = FastAttackSettings.AutoClick and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

btnAutoRaid.MouseButton1Click:Connect(function()
    AutoRaidSettings.Enabled = not AutoRaidSettings.Enabled
    btnAutoRaid.Text = AutoRaidSettings.Enabled and "ON" or "OFF"
    btnAutoRaid.TextColor3 = AutoRaidSettings.Enabled and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
    if not AutoRaidSettings.Enabled then
        local char = localPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if root then
            if root:FindFirstChild("RaidFlyBV") then root.RaidFlyBV:Destroy() end
            if root:FindFirstChild("RaidFlyBG") then root.RaidFlyBG:Destroy() end
        end
        if char then
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") then
                    part.CanCollide = true
                end
            end
        end
    end
end)

btnESP.MouseButton1Click:Connect(function()
    ESPStates.Enabled = not ESPStates.Enabled
    btnESP.Text = ESPStates.Enabled and "ON" or "OFF"
    btnESP.TextColor3 = ESPStates.Enabled and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

local espFolder = CoreGui:FindFirstChild("AlwaysActivePlayerESP")
if not espFolder then
    espFolder = Instance.new("Folder")
    espFolder.Name = "AlwaysActivePlayerESP"
    espFolder.Parent = CoreGui
end

-- FIXED: Crosshair / Screen-Center Camlock with Ally & Visibility Filtering
local function getClosestTarget(isNpc)
    local closestTarget = nil
    local shortestDist = math.huge
    local screenSize = camera.ViewportSize
    local screenCenter = Vector2.new(screenSize.X / 2, screenSize.Y / 2)

    if isNpc then
        local enemiesFolder = Workspace:FindFirstChild("Enemies")
        if enemiesFolder then
            for _, mob in ipairs(enemiesFolder:GetChildren()) do
                if isValidRaidEnemy(mob) then
                    local root = mob:FindFirstChild("HumanoidRootPart")
                    if root then
                        local screenPos = camera:WorldToViewportPoint(root.Position)
                        if screenPos.Z > 0 then
                            local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                            if screenDist < shortestDist then
                                shortestDist = screenDist
                                closestTarget = root
                            end
                        end
                    end
                end
            end
        end
    else
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= localPlayer and plr.Character then
                -- Skip allies on the same team
                if not (localPlayer.Team and plr.Team and localPlayer.Team == plr.Team) then
                    local root = plr.Character:FindFirstChild("HumanoidRootPart")
                    local hum = plr.Character:FindFirstChild("Humanoid")
                    if root and hum and hum.Health > 0 then
                        local screenPos = camera:WorldToViewportPoint(root.Position)
                        if screenPos.Z > 0 then
                            local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - screenCenter).Magnitude
                            if screenDist < shortestDist then
                                shortestDist = screenDist
                                closestTarget = root
                            end
                        end
                    end
                end
            end
        end
    end
    return closestTarget
end

RunService.RenderStepped:Connect(function()
    pcall(function()
        if AutoRaidSettings.Enabled then
            manageRaidFlight()
        end

        if ESPStates.Enabled then
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= localPlayer then
                    local char = plr.Character
                    local root = char and char:FindFirstChild("HumanoidRootPart")
                    local hum = char and char:FindFirstChild("Humanoid")
                    local existingEsp = espFolder:FindFirstChild("ESP_" .. plr.Name)
                    
                    if char and root and hum and hum.Health > 0 then
                        local playerLevel = "1"
                        local stats = plr:FindFirstChild("Data") or plr:FindFirstChild("leaderstats")
                        if stats and stats:FindFirstChild("Level") then playerLevel = tostring(stats.Level.Value) end

                        local espColor = Color3.fromRGB(255, 50, 50)
                        if plr.Team then
                            local teamName = tostring(plr.Team.Name):lower()
                            if teamName:find("marine") then
                                espColor = Color3.fromRGB(50, 150, 255)
                            end
                        end

                        local billboard = existingEsp
                        if not billboard then
                            billboard = Instance.new("BillboardGui")
                            billboard.Name = "ESP_" .. plr.Name
                            billboard.Size = UDim2.new(0, 150, 0, 40)
                            billboard.StudsOffset = Vector3.new(0, 3, 0)
                            billboard.AlwaysOnTop = true
                            billboard.Parent = espFolder
                            
                            local textLbl = Instance.new("TextLabel")
                            textLbl.Name = "TextInfo"
                            textLbl.Size = UDim2.new(1, 0, 1, 0)
                            textLbl.BackgroundTransparency = 1
                            textLbl.Font = Enum.Font.SourceSansBold
                            textLbl.TextSize = 13
                            textLbl.TextStrokeTransparency = 0.2
                            textLbl.Parent = billboard
                        end

                        billboard.Adornee = root
                        local textLbl = billboard:FindFirstChild("TextInfo")
                        if textLbl then
                            textLbl.TextColor3 = espColor
                            textLbl.Text = string.format("%s [Lvl: %s]\n[%d / %d HP]", plr.Name, playerLevel, math.floor(hum.Health), math.floor(hum.MaxHealth))
                        end
                    else
                        if existingEsp then existingEsp:Destroy() end
                    end
                end
            end
        else
            for _, v in ipairs(espFolder:GetChildren()) do v:Destroy() end
        end
        
        if CombatStates.PlayerCamActive then
            if not CombatStates.LockedTarget or not CombatStates.LockedTarget.Parent or not CombatStates.LockedTarget.Parent:FindFirstChild("Humanoid") or CombatStates.LockedTarget.Parent.Humanoid.Health <= 0 then
                CombatStates.LockedTarget = getClosestTarget(false)
            end
            if CombatStates.LockedTarget then
                camera.CFrame = CFrame.new(camera.CFrame.Position, CombatStates.LockedTarget.Position)
            end
        elseif CombatStates.NpcCamActive then
            if not CombatStates.LockedTarget or not CombatStates.LockedTarget.Parent or not CombatStates.LockedTarget.Parent:FindFirstChild("Humanoid") or CombatStates.LockedTarget.Parent.Humanoid.Health <= 0 then
                CombatStates.LockedTarget = getClosestTarget(true)
            end
            if CombatStates.LockedTarget then
                camera.CFrame = CFrame.new(camera.CFrame.Position, CombatStates.LockedTarget.Position)
            end
        end
    end)
end)

task.spawn(function()
    while true do
        pcall(function()
            if FastAttackSettings.AutoClick then
                local char = localPlayer.Character
                if char and char:FindFirstChildOfClass("Tool") then
                    local myRoot = char:FindFirstChild("HumanoidRootPart")
                    if myRoot then
                        local enemiesFolder = Workspace:FindFirstChild("Enemies")
                        if enemiesFolder then
                            local targetsList = {}
                            for _, mob in pairs(enemiesFolder:GetChildren()) do
                                if isValidRaidEnemy(mob) then
                                    local head = mob:FindFirstChild("Head")
                                    local root = mob:FindFirstChild("HumanoidRootPart")
                                    local hum = mob:FindFirstChild("Humanoid")
                                    if head and root and hum and hum.Health > 0 and (root.Position - myRoot.Position).Magnitude <= FastAttackSettings.Distance then
                                        table.insert(targetsList, {Head = head, Root = root})
                                    end
                                end
                            end
                            if #targetsList > 0 then
                                executeMultiAttack(targetsList)
                            end
                        end
                    end
                end
            end
        end)
        task.wait(FastAttackSettings.ClickDelay)
    end
end)

UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    pcall(function()
        local char = localPlayer.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")

        if input.KeyCode == Keybinds.MenuToggle then
            MainFrame.Visible = not MainFrame.Visible
        elseif input.KeyCode == Keybinds.MenuHide then
            ScreenGui.Enabled = not ScreenGui.Enabled
        elseif input.KeyCode == Keybinds.PlayerCam then
            CombatStates.PlayerCamActive = not CombatStates.PlayerCamActive
            if CombatStates.PlayerCamActive then CombatStates.NpcCamActive = false end
            CombatStates.LockedTarget = nil
        elseif input.KeyCode == Keybinds.NpcCam then
            CombatStates.NpcCamActive = not CombatStates.NpcCamActive
            if CombatStates.NpcCamActive then CombatStates.PlayerCamActive = false end
            CombatStates.LockedTarget = nil
        elseif input.KeyCode == Keybinds.TpEnemy then
            teleportBehindEnemy()
        elseif input.KeyCode == Keybinds.SuperJump and root then
            root.AssemblyLinearVelocity = Vector3.new(root.AssemblyLinearVelocity.X, 350, root.AssemblyLinearVelocity.Z)
        elseif input.KeyCode == Keybinds.LongDash and root then
            local lookVec = camera.CFrame.LookVector
            root.AssemblyLinearVelocity = Vector3.new(lookVec.X * 350, 0, lookVec.Z * 350)
        end
    end)
end)

OpenButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
