--[[
    Blox Fruits Hub (All Features + Camlock Keybinds + ESP Toggle Switch)
]]

local CoreGui = game:GetService("CoreGui")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualInputManager = game:GetService("VirtualInputManager")

local localPlayer = Players.LocalPlayer
local camera = Workspace.CurrentCamera
local saveFileName = "BloxFruitHubSettings_" .. localPlayer.Name .. ".json"

-- FORCE-NUKE CLEANUP
local function nukeOldUIs()
    local targetNames = {"KeybindSettingUI", "PvPHUDUI", "PlayerESP", "RaidFarmUI", "StandalonePlayerESP", "AlwaysActivePlayerESP"}
    for _, v in ipairs(CoreGui:GetChildren()) do
        for _, name in ipairs(targetNames) do
            if v.Name == name or v.Name:find("BloxFruit") then
                pcall(function() v:Destroy() end)
            end
        end
    end
    if localPlayer:FindFirstChild("PlayerGui") then
        for _, v in ipairs(localPlayer.PlayerGui:GetChildren()) do
            for _, name in ipairs(targetNames) do
                if v.Name == name or v.Name:find("BloxFruit") then
                    pcall(function() v:Destroy() end)
                end
            end
        end
    end
end

nukeOldUIs()
task.wait(0.1)

-- Default Keybinds Table
local Keybinds = {
    PlayerCam = Enum.KeyCode.P,
    NpcCam = Enum.KeyCode.N,
    SuperJump = Enum.KeyCode.L,
    LongDash = Enum.KeyCode.H,
    TpEnemy = Enum.KeyCode.T,
    TpAlly = Enum.KeyCode.U,
    NoStunKey = Enum.KeyCode.J,
    MenuToggle = Enum.KeyCode.M,
    MenuHide = Enum.KeyCode.B
}

-- Settings States
local FastAttackSettings = { AutoClick = false, ClickDelay = 0.12, Distance = 65 }
local NoStunSettings = { Enabled = false }
local ESPStates = { Enabled = true } -- ESP Toggle State (Default ON)
local PvPStates = { AutoV3 = false, PlayerCamActive = false, NpcCamActive = false, LockedTarget = nil }
local RaidStates = { AutoFarmRaid = false, AutoV4 = false }

-- Remote Finders
local function getRegisterAttack()
    local success, res = pcall(function() return ReplicatedStorage.Modules.Net["RE/RegisterAttack"] end)
    if success and res then return res end
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v.Name == "RegisterAttack" or v.Name == "RE/RegisterAttack" then return v end
    end
    return nil
end

local function getRegisterHit()
    local success, res = pcall(function() return ReplicatedStorage.Modules.Net["RE/RegisterHit"] end)
    if success and res then return res end
    for _, v in ipairs(ReplicatedStorage:GetDescendants()) do
        if v.Name == "RegisterHit" or v.Name == "RE/RegisterHit" then return v end
    end
    return nil
end

local function executeAttack(targetRoot, targetHead)
    local char = localPlayer.Character
    if not char then return end
    local equippedWeapon = char:FindFirstChildOfClass("Tool")
    if equippedWeapon then
        pcall(function()
            local attackRemote = equippedWeapon:FindFirstChild("LeftClickRemote") or equippedWeapon:FindFirstChild("ToolAttack") or equippedWeapon:FindFirstChild("AttackRemote")
            if attackRemote and typeof(attackRemote.FireServer) == "function" and targetRoot then
                local direction = (targetRoot.Position - char:GetPivot().Position).Unit
                attackRemote:FireServer(direction, 1)
            end
        end)
    end
    pcall(function()
        local regAttack = getRegisterAttack()
        if regAttack and typeof(regAttack.FireServer) == "function" then regAttack:FireServer(0) end
        local regHit = getRegisterHit()
        if regHit and typeof(regHit.FireServer) == "function" and targetHead then
            regHit:FireServer(targetHead, { {targetHead.Parent, targetHead} })
        end
    end)
end

local function saveKeybinds()
    if writefile and HttpService then
        local encodedTable = {}
        for keyName, keyCode in pairs(Keybinds) do encodedTable[keyName] = keyCode.Name end
        pcall(function() writefile(saveFileName, HttpService:JSONEncode(encodedTable)) end)
    end
end

local function loadKeybinds()
    if writefile and readfile and isfile and isfile(saveFileName) then
        local success, decoded = pcall(function() return HttpService:JSONDecode(readfile(saveFileName)) end)
        if success and type(decoded) == "table" then
            for keyName, enumName in pairs(decoded) do
                if Keybinds[keyName] ~= nil and Enum.KeyCode[enumName] then Keybinds[keyName] = Enum.KeyCode[enumName] end
            end
        end
    end
end
loadKeybinds()

-- UI Layout Setup
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KeybindSettingUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = CoreGui

local OpenButton = Instance.new("TextButton")
OpenButton.Size = UDim2.new(0, 160, 0, 45)
OpenButton.Position = UDim2.new(0, 20, 0, 100)
OpenButton.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
OpenButton.TextColor3 = Color3.fromRGB(0, 255, 128)
OpenButton.TextSize = 15
OpenButton.Font = Enum.Font.SourceSansBold
OpenButton.Text = "Toggle Menu [M]"
OpenButton.Active = true
OpenButton.Parent = ScreenGui
Instance.new("UICorner", OpenButton).CornerRadius = UDim.new(0, 6)

local frameWidth, frameHeight = 500, 480
local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, frameWidth, 0, frameHeight)
MainFrame.Position = UDim2.new(0.5, -frameWidth/2, 0.5, -frameHeight/2)
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Visible = true 
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 8)

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, 0, 0, 45)
TitleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.TextSize = 18
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.Text = "  Blox Fruits Hub"
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = MainFrame
Instance.new("UICorner", TitleLabel).CornerRadius = UDim.new(0, 8)

local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 130, 1, -55)
Sidebar.Position = UDim2.new(0, 10, 0, 50)
Sidebar.BackgroundTransparency = 1
Sidebar.Parent = MainFrame

local TabPvPBtn = Instance.new("TextButton")
TabPvPBtn.Size = UDim2.new(1, 0, 0, 38)
TabPvPBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
TabPvPBtn.Font = Enum.Font.SourceSansBold
TabPvPBtn.TextSize = 13
TabPvPBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
TabPvPBtn.Text = "PvP Hub"
TabPvPBtn.Parent = Sidebar
Instance.new("UICorner", TabPvPBtn).CornerRadius = UDim.new(0, 6)

local TabRaidBtn = Instance.new("TextButton")
TabRaidBtn.Size = UDim2.new(1, 0, 0, 38)
TabRaidBtn.Position = UDim2.new(0, 0, 0, 48)
TabRaidBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
TabRaidBtn.Font = Enum.Font.SourceSansBold
TabRaidBtn.TextSize = 13
TabRaidBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
TabRaidBtn.Text = "Raid"
TabRaidBtn.Parent = Sidebar
Instance.new("UICorner", TabRaidBtn).CornerRadius = UDim.new(0, 6)

local ContentContainer = Instance.new("Frame")
ContentContainer.Size = UDim2.new(1, -155, 1, -55)
ContentContainer.Position = UDim2.new(0, 145, 0, 50)
ContentContainer.BackgroundTransparency = 1
ContentContainer.Parent = MainFrame

local TabPvP = Instance.new("ScrollingFrame")
TabPvP.Size = UDim2.new(1, 0, 1, 0)
TabPvP.BackgroundTransparency = 1
TabPvP.CanvasSize = UDim2.new(0, 0, 0, 570)
TabPvP.ScrollBarThickness = 4
TabPvP.Visible = true
TabPvP.Parent = ContentContainer

local TabRaid = Instance.new("ScrollingFrame")
TabRaid.Size = UDim2.new(1, 0, 1, 0)
TabRaid.BackgroundTransparency = 1
TabRaid.CanvasSize = UDim2.new(0, 0, 0, 420)
TabRaid.ScrollBarThickness = 4
TabRaid.Visible = false
TabRaid.Parent = ContentContainer

TabPvPBtn.MouseButton1Click:Connect(function()
    TabPvP.Visible = true
    TabRaid.Visible = false
    TabPvPBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    TabPvPBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
    TabRaidBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    TabRaidBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
end)

TabRaidBtn.MouseButton1Click:Connect(function()
    TabRaid.Visible = true
    TabPvP.Visible = false
    TabRaidBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    TabRaidBtn.TextColor3 = Color3.fromRGB(0, 255, 128)
    TabPvPBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    TabPvPBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
end)

local function createToggleRow(parentFrame, name, initialState, yPos)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, 0, 0, 34)
    Row.Position = UDim2.new(0, 0, 0, yPos)
    Row.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    Row.Parent = parentFrame
    Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.6, 0, 1, 0)
    Label.Position = UDim2.new(0, 10, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.SourceSansBold
    Label.TextSize = 13
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Text = name
    Label.Parent = Row

    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(0, 90, 0, 26)
    Button.Position = UDim2.new(1, -95, 0.5, -13)
    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    Button.Font = Enum.Font.SourceSansBold
    Button.TextSize = 12
    Button.Text = initialState and "ON" or "OFF"
    Button.TextColor3 = initialState and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
    Button.Parent = Row
    Instance.new("UICorner", Button).CornerRadius = UDim.new(0, 4)

    return Button
end

local function createKeybindRow(parentFrame, name, keyName, yPos)
    local Row = Instance.new("Frame")
    Row.Size = UDim2.new(1, 0, 0, 34)
    Row.Position = UDim2.new(0, 0, 0, yPos)
    Row.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
    Row.Parent = parentFrame
    Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 6)

    local Label = Instance.new("TextLabel")
    Label.Size = UDim2.new(0.45, 0, 1, 0)
    Label.Position = UDim2.new(0, 10, 0, 0)
    Label.BackgroundTransparency = 1
    Label.Font = Enum.Font.SourceSansBold
    Label.TextSize = 12
    Label.TextColor3 = Color3.fromRGB(220, 220, 220)
    Label.TextXAlignment = Enum.TextXAlignment.Left
    Label.Text = name
    Label.Parent = Row

    local KeyButton = Instance.new("TextButton")
    KeyButton.Size = UDim2.new(0, 110, 0, 26)
    KeyButton.Position = UDim2.new(1, -115, 0.5, -13)
    KeyButton.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
    KeyButton.Font = Enum.Font.SourceSansBold
    KeyButton.TextSize = 12
    KeyButton.Text = Keybinds[keyName].Name
    KeyButton.TextColor3 = Color3.fromRGB(0, 255, 128)
    KeyButton.Parent = Row
    Instance.new("UICorner", KeyButton).CornerRadius = UDim.new(0, 4)

    local listening = false
    KeyButton.MouseButton1Click:Connect(function()
        if listening then return end
        listening = true
        KeyButton.Text = "Press Key..."
        KeyButton.TextColor3 = Color3.fromRGB(255, 200, 0)
        
        local connection
        connection = UserInputService.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.Keyboard then
                Keybinds[keyName] = input.KeyCode
                KeyButton.Text = input.KeyCode.Name
                KeyButton.TextColor3 = Color3.fromRGB(0, 255, 128)
                listening = false
                saveKeybinds()
                connection:Disconnect()
            end
        end)
    end)
end

-- POPULATE PvP TAB
local btnFastAttack = createToggleRow(TabPvP, "Fast Attack", FastAttackSettings.AutoClick, 5)
local btnNoStun = createToggleRow(TabPvP, "No Stun", NoStunSettings.Enabled, 45)
local btnESP = createToggleRow(TabPvP, "ESP Toggle", ESPStates.Enabled, 85)
local btnAutoV3 = createToggleRow(TabPvP, "Auto V3", PvPStates.AutoV3, 125)

-- CAMLOCK KEYBIND SETTERS & OTHERS
createKeybindRow(TabPvP, "Player Camlock Key", "PlayerCam", 165)
createKeybindRow(TabPvP, "NPC Camlock Key", "NpcCam", 205)
createKeybindRow(TabPvP, "Super Jump", "SuperJump", 245)
createKeybindRow(TabPvP, "Long Dash", "LongDash", 285)
createKeybindRow(TabPvP, "Tp to Enemy", "TpEnemy", 325)
createKeybindRow(TabPvP, "Tp to Ally", "TpAlly", 365)
createKeybindRow(TabPvP, "Toggle No Stun", "NoStunKey", 405)
createKeybindRow(TabPvP, "Hide/Unhide UI", "MenuHide", 445)

-- Save Config Row
local SaveRow = Instance.new("Frame")
SaveRow.Size = UDim2.new(1, 0, 0, 34)
SaveRow.Position = UDim2.new(0, 0, 0, 485)
SaveRow.BackgroundColor3 = Color3.fromRGB(30, 30, 38)
SaveRow.Parent = TabPvP
Instance.new("UICorner", SaveRow).CornerRadius = UDim.new(0, 6)

local SaveLabel = Instance.new("TextLabel")
SaveLabel.Size = UDim2.new(0.5, 0, 1, 0)
SaveLabel.Position = UDim2.new(0, 10, 0, 0)
SaveLabel.BackgroundTransparency = 1
SaveLabel.Font = Enum.Font.SourceSansBold
SaveLabel.TextSize = 12
SaveLabel.TextColor3 = Color3.fromRGB(220, 220, 220)
SaveLabel.TextXAlignment = Enum.TextXAlignment.Left
SaveLabel.Text = "Save Config"
SaveLabel.Parent = SaveRow

local SaveButton = Instance.new("TextButton")
SaveButton.Size = UDim2.new(0, 110, 0, 26)
SaveButton.Position = UDim2.new(1, -115, 0.5, -13)
SaveButton.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
SaveButton.Font = Enum.Font.SourceSansBold
SaveButton.TextSize = 12
SaveButton.Text = "Save Now"
SaveButton.TextColor3 = Color3.fromRGB(0, 255, 128)
SaveButton.Parent = SaveRow
Instance.new("UICorner", SaveButton).CornerRadius = UDim.new(0, 4)

SaveButton.MouseButton1Click:Connect(function()
    saveKeybinds()
    SaveButton.Text = "Saved!"
    task.wait(1)
    SaveButton.Text = "Save Now"
end)

-- POPULATE RAID TAB
local btnAutoFarmRaid = createToggleRow(TabRaid, "Auto Farm Raid", RaidStates.AutoFarmRaid, 5)
local btnAutoV4 = createToggleRow(TabRaid, "Auto V4", RaidStates.AutoV4, 45)

-- UI Toggle Event Handlers
btnFastAttack.MouseButton1Click:Connect(function()
    FastAttackSettings.AutoClick = not FastAttackSettings.AutoClick
    btnFastAttack.Text = FastAttackSettings.AutoClick and "ON" or "OFF"
    btnFastAttack.TextColor3 = FastAttackSettings.AutoClick and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

btnNoStun.MouseButton1Click:Connect(function()
    NoStunSettings.Enabled = not NoStunSettings.Enabled
    btnNoStun.Text = NoStunSettings.Enabled and "ON" or "OFF"
    btnNoStun.TextColor3 = NoStunSettings.Enabled and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

btnESP.MouseButton1Click:Connect(function()
    ESPStates.Enabled = not ESPStates.Enabled
    btnESP.Text = ESPStates.Enabled and "ON" or "OFF"
    btnESP.TextColor3 = ESPStates.Enabled and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
    if not ESPStates.Enabled then
        local espFolder = CoreGui:FindFirstChild("AlwaysActivePlayerESP")
        if espFolder then
            for _, v in ipairs(espFolder:GetChildren()) do v:Destroy() end
        end
    end
end)

btnAutoV3.MouseButton1Click:Connect(function()
    PvPStates.AutoV3 = not PvPStates.AutoV3
    btnAutoV3.Text = PvPStates.AutoV3 and "ON" or "OFF"
    btnAutoV3.TextColor3 = PvPStates.AutoV3 and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

btnAutoFarmRaid.MouseButton1Click:Connect(function()
    RaidStates.AutoFarmRaid = not RaidStates.AutoFarmRaid
    btnAutoFarmRaid.Text = RaidStates.AutoFarmRaid and "ON" or "OFF"
    btnAutoFarmRaid.TextColor3 = RaidStates.AutoFarmRaid and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

btnAutoV4.MouseButton1Click:Connect(function()
    RaidStates.AutoV4 = not RaidStates.AutoV4
    btnAutoV4.Text = RaidStates.AutoV4 and "ON" or "OFF"
    btnAutoV4.TextColor3 = RaidStates.AutoV4 and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
end)

-- ESP SYSTEM FOLDER
local espFolder = CoreGui:FindFirstChild("AlwaysActivePlayerESP")
if not espFolder then
    espFolder = Instance.new("Folder")
    espFolder.Name = "AlwaysActivePlayerESP"
    espFolder.Parent = CoreGui
end

-- Function to find closest available Target for Camlock
local function getClosestTarget(isNpc)
    local myChar = localPlayer.Character
    local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myRoot then return nil end

    local closestTarget = nil
    local shortestDist = math.huge

    if isNpc then
        local enemiesFolder = Workspace:FindFirstChild("Enemies")
        if enemiesFolder then
            for _, mob in ipairs(enemiesFolder:GetChildren()) do
                local root = mob:FindFirstChild("HumanoidRootPart")
                local hum = mob:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    local dist = (root.Position - myRoot.Position).Magnitude
                    if dist < shortestDist then
                        shortestDist = dist
                        closestTarget = root
                    end
                end
            end
        end
    else
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= localPlayer and plr.Character then
                local root = plr.Character:FindFirstChild("HumanoidRootPart")
                local hum = plr.Character:FindFirstChild("Humanoid")
                if root and hum and hum.Health > 0 then
                    local dist = (root.Position - myRoot.Position).Magnitude
                    if dist < shortestDist then
                        shortestDist = dist
                        closestTarget = root
                    end
                end
            end
        end
    end
    return closestTarget
end

RunService.RenderStepped:Connect(function()
    -- Render ESP Updates (Only if ESP is enabled)
    if ESPStates.Enabled then
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= localPlayer then
                local char = plr.Character
                local root = char and char:FindFirstChild("HumanoidRootPart")
                local hum = char and char:FindFirstChild("Humanoid")
                local existingEsp = espFolder:FindFirstChild("ESP_" .. plr.Name)
                
                if char and root and hum and hum.Health > 0 then
                    local playerLevel = "1"
                    local stats = plr:FindFirstChild("Data") or plr:FindFirstChild("leaderstats")
                    if stats and stats:FindFirstChild("Level") then playerLevel = tostring(stats.Level.Value) end

                    local espColor = Color3.fromRGB(255, 50, 50)
                    if plr.Team then
                        local tName = string.lower(plr.Team.Name)
                        if string.find(tName, "marine") then espColor = Color3.fromRGB(50, 150, 255) end
                    end

                    local billboard = existingEsp
                    if not billboard then
                        billboard = Instance.new("BillboardGui")
                        billboard.Name = "ESP_" .. plr.Name
                        billboard.Size = UDim2.new(0, 150, 0, 40)
                        billboard.StudsOffset = Vector3.new(0, 3, 0)
                        billboard.AlwaysOnTop = true
                        billboard.Parent = espFolder
                        
                        local textLbl = Instance.new("TextLabel")
                        textLbl.Name = "TextInfo"
                        textLbl.Size = UDim2.new(1, 0, 1, 0)
                        textLbl.BackgroundTransparency = 1
                        textLbl.Font = Enum.Font.SourceSansBold
                        textLbl.TextSize = 13
                        textLbl.TextStrokeTransparency = 0.2
                        textLbl.Parent = billboard
                    end

                    billboard.Adornee = root
                    local textLbl = billboard:FindFirstChild("TextInfo")
                    if textLbl then
                        textLbl.TextColor3 = espColor
                        textLbl.Text = string.format("%s [Lvl: %s]\n[%d / %d HP]", plr.Name, playerLevel, math.floor(hum.Health), math.floor(hum.MaxHealth))
                    end
                else
                    if existingEsp then existingEsp:Destroy() end
                end
            end
        end
    else
        for _, v in ipairs(espFolder:GetChildren()) do v:Destroy() end
    end
    
    -- Camlock Tracking Execution
    if PvPStates.PlayerCamActive then
        if not PvPStates.LockedTarget or not PvPStates.LockedTarget.Parent or PvPStates.LockedTarget.Parent:FindFirstChild("Humanoid").Health <= 0 then
            PvPStates.LockedTarget = getClosestTarget(false)
        end
        if PvPStates.LockedTarget then
            camera.CFrame = CFrame.new(camera.CFrame.Position, PvPStates.LockedTarget.Position)
        end
    elseif PvPStates.NpcCamActive then
        if not PvPStates.LockedTarget or not PvPStates.LockedTarget.Parent or PvPStates.LockedTarget.Parent:FindFirstChild("Humanoid").Health <= 0 then
            PvPStates.LockedTarget = getClosestTarget(true)
        end
        if PvPStates.LockedTarget then
            camera.CFrame = CFrame.new(camera.CFrame.Position, PvPStates.LockedTarget.Position)
        end
    end
end)

-- Background Loops
task.spawn(function()
    while true do
        if FastAttackSettings.AutoClick then
            local char = localPlayer.Character
            if char and char:FindFirstChildOfClass("Tool") then
                local myRoot = char:FindFirstChild("HumanoidRootPart")
                for _, plr in ipairs(Players:GetPlayers()) do
                    if plr ~= localPlayer and plr.Character then
                        local root = plr.Character:FindFirstChild("HumanoidRootPart")
                        local head = plr.Character:FindFirstChild("Head")
                        local hum = plr.Character:FindFirstChild("Humanoid")
                        if root and head and hum and hum.Health > 0 and myRoot and (root.Position - myRoot.Position).Magnitude <= FastAttackSettings.Distance then
                            executeAttack(root, head)
                        end
                    end
                end

                local enemiesFolder = Workspace:FindFirstChild("Enemies")
                if enemiesFolder then
                    for _, mob in pairs(enemiesFolder:GetChildren()) do
                        local head = mob:FindFirstChild("Head")
                        local root = mob:FindFirstChild("HumanoidRootPart")
                        local hum = mob:FindFirstChild("Humanoid")
                        if head and root and hum and hum.Health > 0 and myRoot and (root.Position - myRoot.Position).Magnitude <= FastAttackSettings.Distance then
                            executeAttack(root, head)
                        end
                    end
                end
            end
        end
        task.wait(FastAttackSettings.ClickDelay)
    end
end)

task.spawn(function()
    while true do
        task.wait(0.1)
        if NoStunSettings.Enabled then
            pcall(function()
                local char = localPlayer.Character
                if char then
                    if char:FindFirstChild("Stun") then char.Stun.Value = 0 end
                    if char:FindFirstChild("Busy") then char.Busy.Value = false end
                end
            end)
        end
    end
end)

task.spawn(function()
    while true do
        task.wait(1)
        pcall(function()
            if PvPStates.AutoV3 or RaidStates.AutoV4 then
                VirtualInputManager:SendKeyEvent(true, Enum.KeyCode.T, false, game)
                task.wait(0.1)
                VirtualInputManager:SendKeyEvent(false, Enum.KeyCode.T, false, game)
                if ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("CommE_") then
                    ReplicatedStorage.Remotes.CommE_:FireServer("ActivateAbility")
                end
            end
        end)
    end
end)

-- KEYBIND INPUT LISTENER
UserInputService.InputBegan:Connect(function(input, gp)
    if gp then return end
    local char = localPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")

    if input.KeyCode == Keybinds.MenuToggle then
        MainFrame.Visible = not MainFrame.Visible
    elseif input.KeyCode == Keybinds.MenuHide then
        ScreenGui.Enabled = not ScreenGui.Enabled
    elseif input.KeyCode == Keybinds.PlayerCam then
        PvPStates.PlayerCamActive = not PvPStates.PlayerCamActive
        if PvPStates.PlayerCamActive then PvPStates.NpcCamActive = false end
        PvPStates.LockedTarget = nil
    elseif input.KeyCode == Keybinds.NpcCam then
        PvPStates.NpcCamActive = not PvPStates.NpcCamActive
        if PvPStates.NpcCamActive then PvPStates.PlayerCamActive = false end
        PvPStates.LockedTarget = nil
    elseif input.KeyCode == Keybinds.NoStunKey then
        NoStunSettings.Enabled = not NoStunSettings.Enabled
        btnNoStun.Text = NoStunSettings.Enabled and "ON" or "OFF"
        btnNoStun.TextColor3 = NoStunSettings.Enabled and Color3.fromRGB(0, 255, 128) or Color3.fromRGB(255, 50, 50)
    elseif input.KeyCode == Keybinds.SuperJump and root then
        root.AssemblyLinearVelocity = Vector3.new(root.AssemblyLinearVelocity.X, 350, root.AssemblyLinearVelocity.Z)
    elseif input.KeyCode == Keybinds.LongDash and root then
        local lookVec = camera.CFrame.LookVector
        root.AssemblyLinearVelocity = Vector3.new(lookVec.X * 350, 0, lookVec.Z * 350)
    end
end)

OpenButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)
